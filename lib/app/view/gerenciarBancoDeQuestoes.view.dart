import 'package:flutter/material.dart';

class GerenciarBancoDeQuestoes extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Gerenciar bancos de questões'),
      ),
      body: Center(child: Text('Conteúdo da página de bancos de questões')),
    );
  }
}
